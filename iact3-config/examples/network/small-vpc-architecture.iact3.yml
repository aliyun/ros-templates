project:
  auth:
    name: akProfile
    location: ~/.aliyun/config.json
  name: test-small-vpc-architecture
  regions:
    - cn-hangzhou
  oss_config:
    bucket_name: test-yx-hangzhou
    bucket_region: cn-hangzhou
tests:
  default:
    parameters:
      VSwitchZoneId1: $[iact3-auto]
      VSwitchZoneId2: $[iact3-auto]
      InstanceType: 'ecs.g6.large'
      Password: $[iact3-auto]

